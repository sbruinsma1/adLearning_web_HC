rsrecovr::recovr()
import(rsrecovr)
require(rsrecover)
remotes::install_github("jmcphers/rsrecovr")
rsrecovr::recovr()
rsrecovr::recovr(project_path = NULL)
rsrecovr::recovr(project_path = "all")
force = TRUE
rsrecovr::recovr(project_path = "all")
rsrecovr::recovr(project_path = "all", force = TRUE)
